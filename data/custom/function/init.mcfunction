# This file will run all commands inside whenever the world gets loaded 
# (leaving and re-entering will cause it to run again, or do the /reload command)
# I usually use this to set up scoreboards, as they only need to be created once.

# Do not put a '/' before the command. You will get an error.
# The commands run sequentially. the first line will run first, then the second, and so on.