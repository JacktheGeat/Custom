# If you put your giant, one-command creation here, it should be able to run. 
# If it is STILL yelling at you for being too long, simply put in each sub-command separated by a line.
# to run this function, you need to run the following command: '/function custom:one_command_creation'

# Do not put a '/' before the command. You will get an error.
# The commands run sequentially. the first line will run first, then the second, and so on.